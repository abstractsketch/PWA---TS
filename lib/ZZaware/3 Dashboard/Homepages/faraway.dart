import 'package:flutter/material.dart';

class Faraway extends StatelessWidget {
  const Faraway ({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(' '),
      ),
      body: const Center(
        child: Text('Inhalt der Seite'),
      ),
    );
  }
}